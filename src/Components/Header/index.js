import React from 'react';

export default () => {
    return (
        <div className="container mt-5 d-flex justify-content-center">
        <h1>API: Video Game database on demand!</h1>
        </div>
    );
}